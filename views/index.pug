extends layout

block content
  h1= title
  ul.list-unstyled
    if show
      small.text-muted= quotes[show].student
      li= quotes[show].data
    else
      each quote in quotes
        small.text-muted= quote.student
        li= quote.data
